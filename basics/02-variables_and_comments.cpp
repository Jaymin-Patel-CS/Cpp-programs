#include <iostream>

using namespace std;

int main() {
    int a = 5;
    cout << "i am ..." << a;
    // we can use the double forword slash for comment out the sentences.
    // same as c.
    // and for multiline comment we use /* */
    return 0;
}