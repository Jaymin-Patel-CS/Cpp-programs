#include<iostream>
using namespace std;

int main()
{
    // the operator precedence of the c++ is same as c.
    // firstly we see the precedence if it comes same then we see associativity.
    return 0;
}
